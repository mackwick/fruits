<!DOCTYPE html>
<html lang="en">
<%- include("../partials/head.ejs") %>
<body>
    <%- include("../partials/header.ejs") %>
    <a href="/fruits/new"><button>Create a Fruit</button></a>
    <main>
        <div>
            <% for (let fruit of fruits) { %>
                <a href="/fruits/<%= fruit._id %>">
                    <h2><%= fruit.name %> - <%= fruit.readyToEat ? "Ripe" : "Not Ripe" %>
                    </h2>
                </a>
            <% } %>
        </div>
    </main>

</body>
</html>